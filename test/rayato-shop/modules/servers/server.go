package servers

type server struct {}

func NewServer() *server {
	return &server{}
}

func (s *server) Start() {
	// Map all routes

	// Server config

	// Start server
}

func (s *server) mapHandlers() error {
	// Middleware list

	// Endpoint list

	// End point not found error response
	return nil
}